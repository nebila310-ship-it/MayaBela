import 'package:flutter/material.dart';

/// Bright sky-blue palette for the teacher role (dashboard, login, class tools).
class TeacherTheme {
  TeacherTheme._();

  static const primary = Color(0xFF1A73E8);
  static const primaryLight = Color(0xFF12B5CB);
  static const primaryDark = Color(0xFF174EA6);
  static const homeroomAccent = Color(0xFF8E24AA);
  static const surface = Color(0xFFE8F0FE);
  static const surfaceWarm = Color(0xFFE6F4EA);

  static const gradient = [
    Color(0xFF174EA6),
    Color(0xFF1A73E8),
    Color(0xFF12B5CB),
  ];
}
