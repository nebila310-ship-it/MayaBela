import 'package:flutter/material.dart';

/// Bright sky-blue palette for the teacher role (dashboard, login, class tools).
class TeacherTheme {
  TeacherTheme._();

  static const primary = Color(0xFF0EA5E9);
  static const primaryLight = Color(0xFF38BDF8);
  static const primaryDark = Color(0xFF0284C7);
  static const homeroomAccent = Color(0xFF6366F1);
  static const surface = Color(0xFFF0F9FF);
  static const surfaceWarm = Color(0xFFE0F2FE);

  static const gradient = [
    Color(0xFF0284C7),
    Color(0xFF0EA5E9),
    Color(0xFF7DD3FC),
  ];
}
